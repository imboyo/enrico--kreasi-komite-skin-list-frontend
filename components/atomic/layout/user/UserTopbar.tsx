// Topbar variant for the logged-in user app pages.
// Extend by passing rightSection (e.g. profile avatar, notifications).
"use client";

import { BaseTopbar } from "@/components/atomic/organism/topbar/BaseTopbar";

export function UserTopbar() {
  return <BaseTopbar />;
}
