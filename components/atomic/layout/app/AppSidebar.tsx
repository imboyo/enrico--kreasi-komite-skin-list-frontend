// Sidebar variant for app pages.
// This starts as an empty shell so the app section can be wired before menu content is implemented.
"use client";

import { BaseSidebar } from "@/components/atomic/organism/sidebar/BaseSidebar";

export function AppSidebar() {
  return <BaseSidebar title="App Menu" />;
}
