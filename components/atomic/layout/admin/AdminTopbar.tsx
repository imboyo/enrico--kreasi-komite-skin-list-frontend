// Topbar variant for admin pages.
// Extend by passing rightSection (e.g. admin badge, settings icon).
"use client";

import { BaseTopbar } from "@/components/atomic/organism/topbar/BaseTopbar";

export function AdminTopbar() {
  return <BaseTopbar />;
}
